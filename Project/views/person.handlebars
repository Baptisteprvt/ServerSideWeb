
<h2> All about {{person.name}}</h2> 

 

<h3>Name {{person.name}}</h3> 

 

{{#if person.dob}} 

<h3>Date of Birth {{person.dob}}</h3> 

{{/if}} 

<img id="img" src={{person.imageurl}} alt="photo of the person">

<ul>
  {{#each person.hog}}
    <li>{{this}}</li>
  {{/each}}
</ul>
<a href='{{person.name}}/edit'>
<button id="Edit" type="button" class="btn btn-primary">Edit</button></a>
<br>
<a href='/family/{{person.name}}/delete'
 onclick="return confirm('are you sure you want to delete this person?');">
<button id="Delete" type="button" class="btn btn-danger">Delete</button> </a>

